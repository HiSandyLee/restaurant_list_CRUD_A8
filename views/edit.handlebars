<div class="banner"></div>
<form action="/restaurants/{{restaurant._id}}/edit" method="POST">

  <div class="container mt-5 d-flex justify-content-center ">
    <div class="card" style="width: 18rem;">
      <div class="card-body px-1 ">
        <table summary="新增餐廳">
          <colgroup width="30%"></colgroup>
          <colgroup width="70%"></colgroup>
          <tbody>
            <tr>
              <td>餐廳名稱</td>
              <td class="add restaurant">
                <input type="text" placeholder="{{restaurant.name}}" name="{{restaurant.name}}">
              </td>
            </tr>
          </tbody>
        </table>

        <table summary="新增餐廳">
          <colgroup width="30%"></colgroup>
          <colgroup width="70%"></colgroup>
          <tbody>
            <tr>
              <td>餐廳英文</td>
              <td class="add restaurant">
                <input type="text" placeholder="{{restaurant.name_en}}" name="{{restaurant.name_en}}">
              </td>
            </tr>
          </tbody>
        </table>
        <table summary="新增餐廳">
          <colgroup width="30%"></colgroup>
          <colgroup width="70%"></colgroup>
          <tbody>
            <tr>
              <td>餐廳總類</td>
              <td class="add restaurant">
                <input type="text" placeholder="{{restaurant.category}}" name="{{restaurant.category}}">
              </td>
            </tr>
          </tbody>
        </table>

        <table summary="新增餐廳">
          <colgroup width="30%"></colgroup>
          <colgroup width="70%"></colgroup>
          <tbody>
            <tr>
              <td>餐廳圖片</td>
              <td class="add restaurant">
                <input type="url" placeholder="{{restaurant.image}}" name="{{restaurant.image}}">
              </td>
            </tr>
          </tbody>
        </table>

        <table summary="新增餐廳">
          <colgroup width="30%"></colgroup>
          <colgroup width="70%"></colgroup>
          <tbody>
            <tr>
              <td>餐廳位置</td>
              <td class="add restaurant">
                <input type="text" placeholder="{{restaurant.location}}" name="{{restaurant.location}}">
              </td>
            </tr>
          </tbody>
        </table>

        </table>
        <table summary="新增餐廳">
          <colgroup width="30%"></colgroup>
          <colgroup width="70%"></colgroup>
          <tbody>
            <tr>
              <td>餐廳評價</td>
              <td class="add restaurant">
                <input type="text" placeholder="{{restaurant.rating}}" name="{{restaurant.rating}}">
              </td>
            </tr>
          </tbody>
        </table>

        <table summary="新增餐廳">
          <colgroup width="30%"></colgroup>
          <colgroup width="70%"></colgroup>
          <tbody>
            <tr>
              <td>餐廳描述</td>
              <td class="add restaurant">
                <input type="text" placeholder="{{restaurant.description}}" name="{{restaurant.description}}">
              </td>
            </tr>
          </tbody>

        </table>

      </div>





    </div>
  </div>


  </div>

  {{!-- buttom --}}
  <div class="d-flex justify-content-center mt-3">
    <button type="submit" class="btn btn-info btn-sm mr-1">Save</button>
    <a href="/" class="btn btn-info btn-sm">Back</a>

  </div>
</form>
</div>